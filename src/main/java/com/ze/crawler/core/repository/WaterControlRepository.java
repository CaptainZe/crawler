package com.ze.crawler.core.repository;

import com.ze.crawler.core.entity.WaterControl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WaterControlRepository extends JpaRepository<WaterControl, String> {
}
